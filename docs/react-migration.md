# Migración a React y TypeScript

- Migrar inicio y política de privacidad a componentes React.
- Mantener el estilo existente usando Bootstrap desde npm y CSS propio.
- Sustituir Font Awesome por `react-icons`.
- Gestionar español e inglés mediante `react-i18next` y archivos JSON.
- Consumir juegos, equipo y redes sociales desde archivos JSON.
- Excluir la página `ggj25` y todos sus componentes.
